'use strict'

import Component from '../components/base_component';
export default class SideBarComponent extends Component {
  constructor(parentSelector, elementSelector) {
    super(parentSelector, elementSelector);
  }
}